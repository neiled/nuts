<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'N.U.T.S', root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <form class="navbar-form navbar-left" role="form">
        <% if user_signed_in? %>
          <%= link_to "Sign Out", destroy_user_session_path, class: "btn btn-default", method: :delete %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "btn btn-primary" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success" %>
        <% end %>        
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to "Talent", talents_path %></li>
          <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Breakdown <span class="badge"><%= breakdowns.count %></span> <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu" aria-labelledby="ddBreakdown">
              <li><%= link_to "New Breakdown...", new_breakdown_path %></li>
              <li class="divider"></li>
              <% for item in breakdowns %>
                <li><%= link_to item.name, item %></li>
              <% end %>
            </ul>
          </li>
        <% end %>
      </ul>
    </div><!--/.navbar-collapse -->
  </div>
</nav>
