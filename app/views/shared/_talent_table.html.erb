<!-- <table class="table">
  <th>Photo</th>
  <th>Surname</th>
  <th>First Name</th>
  <th>Middle Name</th>
  <th>D.O.B</th>
  <th>Permit Expires</th>
  <th>Edit</th>
  <th>Delete</th>

  <% for item in @talents %>
    <tr>
      <td><%= raw get_profile_picture(item) %></td>
      <td><%= link_to item.last_name, talent_path(item) %></td>
      <td><%= item.first_name %></td>
      <td><%= item.middle_name %></td>
      <td><%= item.date_of_birth %></td>
      <td><%= item.permit_expires %></td>
      <td> <%= link_to "Edit", edit_talent_path(item), class: "btn btn-default" %>
      <td> <%= link_to "Delete", talent_path(item), method: :delete, class: "btn btn-danger" %>
    </tr>
  <% end %>

</table> -->



<%= grid(@talent_grid) do |g|

  g.column  name: 'First', attribute: 'first_name'
  g.column name: 'Last', attribute: 'last_name'
  g.column name: 'D.O.B', attribute: 'date_of_birth'
  g.column name: 'Age', attribute: 'date_of_birth', filter: false do |t|
    age(t)
  end
  g.column do |t|
    link_to "Delete", talent_path(t), method: :delete, class: "btn btn-danger btn-sm"
  end
end -%>
